<%= form_for commentable.comments.build do |f| %>
    <%= f.text_field :comment, class: 'form-control' %>
    <%= f.hidden_field :commentable_id %>
    <%= f.hidden_field :commentable_type %>
    <%= f.submit "Comentar", class: 'btn btn-success' %>
<% end %>