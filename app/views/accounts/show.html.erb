
      <div class="page-header">
        <h1>Mi perfil</h1>
      </div>

<div class="row">
    <div class="col-sm-6 col-md-4">
      <div class="thumbnail">
        <%= image_tag current_user.avatar.url(:medium), class: "img-rounded media-picture", alt: current_user.username, data: {original: current_user.avatar.url(:big)} %>
        <div class="caption">
          <h2><%= current_user.username %></h2>
          <p><small><strong>¿Quién es?</strong> <%= Rumoji.decode(current_user.description) %></small></p>
        </div>
      </div>
    </div>
</div>

<div class="row">
  <div class="col-md-12">
    <%= link_to edit_account_path do %>
      <button type="button" class="btn"><span class="glyphicon glyphicon-wrench"></span> Editar perfil</button>
    <% end %>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <%= link_to log_out_path do %>
      <button type="button" class="btn btn-warning"><span class="glyphicon glyphicon-log-out"></span> Salir</button>
    <% end %>
  </div>
</div>

</div>
