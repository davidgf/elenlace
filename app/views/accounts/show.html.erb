
      <div class="page-header">
        <h1>Mi perfil</h1>
      </div>

<div class="row">
    <div class="col-sm-6 col-md-4">
      <div class="thumbnail">
        <%= image_tag current_attendee.avatar.url(:medium), class: "img-rounded media-picture", alt: current_attendee.username, data: {original: current_attendee.avatar.url(:big)} %>
        <div class="caption">
          <h2><%= current_attendee.username %></h2>
          <p><small><strong>¿Quién es?</strong> <%= current_attendee.description %></small></p>
        </div>
      </div>
    </div>
</div>

<div class="row">
  <div class="col-md-12">
    <%= link_to edit_account_path do %>
      <button type="button" class="btn"><span class="glyphicon glyphicon-wrench"></span> Editar perfil</button>
    <% end %>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <%= link_to log_out_path do %>
      <button type="button" class="btn btn-warning"><span class="glyphicon glyphicon-log-out"></span> Salir</button>
    <% end %>
  </div>
</div>

</div>
