<h1>Listing admin_weddings</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @weddings.each do |admin_wedding| %>
      <tr>
        <td><%= link_to 'Show', admin_wedding_path(admin_wedding) %></td>
        <td><%= link_to 'Edit', edit_admin_wedding_path(admin_wedding) %></td>
        <td><%= link_to 'Destroy', admin_wedding_path(admin_wedding), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Wedding', new_admin_wedding_path %>
