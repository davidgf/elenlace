<%= form_for(@admin_wedding) do |f| %>
  <% if @admin_wedding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_wedding.errors.count, "error") %> prohibited this admin_wedding from being saved:</h2>

      <ul>
      <% @admin_wedding.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
